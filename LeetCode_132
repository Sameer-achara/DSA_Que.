class Solution {
public:
    int singleNumber(vector<int>& arr) {
        int size=arr.size()-1;
        int ans=0;
        for(int i=0;i<=size;i++){
            ans=arr[i]^ans;
        }
        return ans;
    }
};
